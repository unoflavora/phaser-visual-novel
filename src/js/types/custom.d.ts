declare namespace Agate {
    namespace Phaser.Math {
        type RangeNumber = {
            min: Number,
            max: Number
        }

        type size = {
            width: Number,
            height: Number
        }

        type positionAndSize = {
            x: number,
            y: number,
            width: number,
            height: number
        }
    }
}