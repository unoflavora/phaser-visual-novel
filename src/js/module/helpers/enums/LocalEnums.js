/**
 * @brief an enum for local storage
 * @typedef {self}
*/
export const ELocalStorage = Object.freeze({
    ALIAS: {
        /**
         * @type {string}
        */
        SCENE_INDEX: `_dslthr-crntscnidx`,
    },
});
